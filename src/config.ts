const global_height = 610;
const global_width = 1080;

const Config = {
    height : global_height,
    width : global_width,
    centerX : global_width / 2,
    centerY : global_height / 2
}
export default Config